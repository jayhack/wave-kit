# Wave Kit showcase

The public documentation and component showcase for `wave-kit`.

## Stack

- Next.js
- Vercel
- Tailwind CSS
- shadcn/ui-compatible application primitives

## Local development

```bash
npm install
npm run dev
npm run build
npm test
```

The workspace root builds the package first so the showcase always consumes the
same distributable code that other projects install.
